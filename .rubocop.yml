inherit_gem:
  rubocop-legion: config/lex.yml

# extinction runner uses early returns from private methods by design
Legion/Extension/RunnerReturnHash:
  Enabled: false

# helpers/actors use defensive logging guards for spec-context compatibility
Legion/HelperMigration/LoggingGuard:
  Enabled: false
